---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: openbao
  namespace: openbao
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    external-dns.alpha.kubernetes.io/hostname: openbao.h-lab.org
spec:
  gatewayClassName: cilium
  listeners:
    - protocol: HTTPS
      port: 443
      name: openbao-https
      hostname: openbao.h-lab.org
      tls:
        mode: Terminate
        certificateRefs:
          - name: openbao-tls
      allowedRoutes:
        namespaces:
          from: Same
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: openbao
  namespace: openbao
spec:
  parentRefs:
    - name: openbao-gateway
      namespace: openbao
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: openbao-ui
          port: 8200
  hostnames:
    - openbao.h-lab.org
