---
apiVersion: external-secrets.io/v1
kind: ClusterExternalSecret
metadata:
  name: k8up-repo-password
spec:
  refreshTime: 24h
  externalSecretName: k8up-repo-password
  externalSecretSpec:
    target:
      name: k8up-repo-password #secret in cluter
    data:
      - secretKey: password #secretKey in cluster
        remoteRef:
          key: k8up-repo-password #key in provider
          property: password #property in provider
          decodingStrategy: None # can be None, Base64, Base64URL or Auto
    secretStoreRef:
      kind: ClusterSecretStore
      name: openbao-backend
  # when using ClusterExternalSecret, you can specify which namespaces can access the secret in this way:
  namespaceSelectors:
    - matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: In
          values:
            - k8up
